# Local overrides - this file is not committed to version control
# Rename to docker-compose.override.yml and customize as needed
version: "3.9"

services:
  backend:
    environment:
      - NODE_ENV=development
    ports:
      - "3000:3000"
